% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hist_graph.R
\name{hist_graph}
\alias{hist_graph}
\title{Styled histogram}
\usage{
hist_graph(
  .data,
  x,
  fill = NULL,
  log_x = FALSE,
  bins = 50,
  title = ggplot2::waiver(),
  subtitle = ggplot2::waiver(),
  xlab = ggplot2::waiver(),
  ylab = ggplot2::waiver(),
  caption = ggplot2::waiver(),
  legend.position = "none"
)
}
\arguments{
\item{.data}{A data frame.}

\item{x}{<[`data-masking`][rlang::args_data_masking]> Bare column name for
the x aesthetic (required).}

\item{fill}{<[`data-masking`][rlang::args_data_masking]> Optional bare
column name to map to the fill aesthetic. When omitted, bars are filled
with the fixed navy colour. When supplied,
`hrbrthemes::scale_fill_ipsum()` is applied.}

\item{log_x}{Logical. If `TRUE`, applies `ggplot2::scale_x_log10()` with
log-spaced breaks and comma labels. Default `FALSE` (linear scale with
`hrbrthemes::scale_x_comma()`).}

\item{bins}{Integer. Number of histogram bins. Passed to
[ggplot2::geom_histogram()]. Default `50`.}

\item{title}{Plot title. Passed to [ggplot2::labs()]. Defaults to
`ggplot2::waiver()` (no title).}

\item{subtitle}{Plot subtitle. Defaults to `ggplot2::waiver()`.}

\item{xlab}{X-axis label. Defaults to `ggplot2::waiver()` (column name).}

\item{ylab}{Y-axis label. Defaults to `ggplot2::waiver()` (column name).}

\item{caption}{Caption below the plot. Defaults to `ggplot2::waiver()`.}

\item{legend.position}{Legend position string passed to
[ggplot2::theme()]. Default `"none"`.}
}
\value{
A `ggplot` object.
}
\description{
A ggplot2 wrapper that produces a consistently-styled histogram using the
`hrbrthemes::theme_ipsum_rc()` theme and `dfr_cols("omd_navy")` as the default
fill colour. Supports optional fill mapping and log-scaled x-axis.
}
\examples{
data.frame(
    id = replicate(
       500,
       paste(sample(letters, 3, replace = TRUE), collapse=""),
       simplify=T),
     n = rnorm(500, mean=50, sd = 10)
) |>
hist_graph(n, title = "Normal Distribution")

}
